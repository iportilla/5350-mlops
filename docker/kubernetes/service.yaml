apiVersion: v1
kind: Service
metadata:
  name: spam-classifier-service
spec:
  selector:
    app: spam-classifier
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
